// Functions

@require 'variables';

fitcontent( $fallback = 50% ) {
    width: $fallback;
    width: intrinsic;
    width: -moz-fit-content;
    width: -webkit-fit-content;
    width: fit-content;
}
